<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >


<mapper namespace="com.management.product.model.dao.ProductDAO">

    <resultMap id="resultMap" type="com.management.product.model.dto.ProductDTO">
        <id property="productCode" column="PRODUCT_CODE"/>
        <result property="productName" column="PRODUCT_NAME"/>
        <result property="originCost" column="ORIGIN_COST"/>
        <result property="releaseDate" column="RELEASE_DATE"/>
        <result property="discountRate" column="DISCOUNT_RATE"/>
        <result property="salesQuantity" column="SALES_QUANTITY"/>
        <result property="stockQuantity" column="STOCK_QUANTITY"/>
        <result property="categoryCode" column="CATEGORY_CODE"/>
        <result property="productionStatus" column="PRODUCTION_STATUS"/>
    </resultMap>

    <select id="selectAllProductList" resultMap="resultMap">
        SELECT PRODUCT_CODE,
               PRODUCT_NAME,
               ORIGIN_COST,
               RELEASE_DATE,
               DISCOUNT_RATE,
               SALES_QUANTITY,
               STOCK_QUANTITY,
               CATEGORY_CODE,
               PRODUCTION_STATUS
               FROM PRODUCT_INFO
    </select>

    <select id="selectProductByCondition" parameterType="com.common.SearchCondition" resultMap="resultMap">
        SELECT PRODUCT_CODE,
               PRODUCT_NAME,
               ORIGIN_COST,
               RELEASE_DATE,
               DISCOUNT_RATE,
               SALES_QUANTITY,
               STOCK_QUANTITY,
               CATEGORY_CODE,
               PRODUCTION_STATUS
        FROM PRODUCT_INFO

        <choose>
            <when test="searchOption == 'productName'">
            WHERE PRODUCT_NAME LIKE CONCAT('%',#{searchValue},'%')
        </when>
            <when test="searchValue == '백화점'"> WHERE DISCOUNT_RATE = 0 </when>
            <when test="searchValue == '아울렛'"> WHERE DISCOUNT_RATE >= 1 </when>
            <when test="searchOption == 'newProduct'">
                WHERE DATE_FORMAT(RELEASE_DATE, "%Y-%m") = DATE_FORMAT(CURDATE(), "%Y-%m")
            </when>
            <when test="searchOption == 'nonProduction'">
                WHERE PRODUCTION_STATUS = 'N'
            </when>
        <otherwise>
            WHERE 1=0
        </otherwise>

        </choose>

    </select>
    <insert id="registNewProduct" parameterType="com.common.SearchCondition">
        INSERT INTO PRODUCT_INFO(
                                 PRODUCT_NAME,
                                 CATEGORY_CODE,
                                 ORIGIN_COST,
                                 RELEASE_DATE,
                                 STOCK_QUANTITY,
                                 SALES_QUANTITY,
                                 DISCOUNT_RATE,
                                 PRODUCTION_STATUS
        )
        VALUES (
                   #{productName},
                   #{categoryCode},
                   #{originCost},
                   DATE_FORMAT(#{releaseDate},"%Y%m%d"),
                   #{stockQuantity},
                   #{salesQuantity},
                   #{discountRate},
                   #{productionStatus}
               )
    </insert>

    <update id="modifyProductInfo" parameterType="com.common.SearchCondition">
        UPDATE PRODUCT_INFO
          <set>
            <if test="productName != null and productName != 'SKIP'">
                ,PRODUCT_NAME = #{ productName }
            </if>
            <if test="originCost != null and originCost != 'SKIP'">
                ,ORIGIN_COST = #{ originCost }
            </if>
              <if test="releaseDate != null and releaseDate != 'SKIP'">
                ,RELEASE_DATE = DATE_FORMAT(#{releaseDate},"%Y%m%d")

              </if>
              <if test="discountRate != null and discountRate != 'SKIP'">
                  ,DISCOUNT_RATE = #{discountRate}
              </if>
              <if test="salesQuantity != null and salesQuantity != 'SKIP'">
                  ,SALES_QUANTITY = #{salesQuantity}
              </if>
              <if test="stockQuantity != null and stockQuantity != 'SKIP'">
                  ,STOCK_QUANTITY = #{stockQuantity}
              </if>
              <if test="categoryCode != null and categoryCode != 'SKIP'">
                  ,CATEGORY_CODE = #{categoryCode}
              </if>
              <if test="productionStatus != null and productionStatus != 'SKIP'">
                  ,PRODUCTION_STATUS = #{productionStatus}
              </if>

          </set>
        WHERE PRODUCT_CODE = #{productCode}

    </update>

    <delete id="deleteProduct" parameterType="hashmap">
        DELETE FROM PRODUCT_INFO
        WHERE PRODUCT_CODE = #{productCode}

    </delete>



</mapper>
